{
  "name": "dog-script",
  "version": "1.0.0",
  "description": "This is a script to fetch data from different apis, combine them into a single jason file and upload it to mongodb",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.26.1",
    "mongodb": "^4.5.0"
  }
}
